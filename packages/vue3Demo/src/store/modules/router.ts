// initial state
const state = {
  all: [],
};

// getters
const getters = {};

// actions
const actions = {
  getAllProducts() {},
};

// mutations
const mutations = {
  setProducts() {},
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};

import stronge from '/@utils/stronge'

const state = {
  all: [],
};

const getters = {};

const actions = {
  getAllProducts() {},
};

const mutations = {
  setProducts() {},
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
